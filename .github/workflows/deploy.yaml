name: Deploy to server

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    env:
      NAME: CodigoFacilito

    steps:
      - name: Working dir
        run: echo $GITHUB_OUTPUT

  second_job:
    needs: first_job
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar Rama
        if: true
        run: 'echo "Rama: ${{github.ref}}"'

